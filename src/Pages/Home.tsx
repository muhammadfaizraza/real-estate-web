import React from "react";
import PropertyList from "../Components/PropertyCard/PropertyList.tsx";

type Props = {};

const Home = (props: Props) => {
  return <PropertyList />;
};

export default Home;
